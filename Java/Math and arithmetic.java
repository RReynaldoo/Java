	//*************************************************  DETAILS  *********************************************************
	int x =1, y = 1; 
	x++;			//Adds 1
	x--;			//Subtracts 1
	x+=1;			// short cut || Works with all operands + - / * %	

	//****************************************  Math methods (some only work with integers)  ******************************
	Math.max(x,y);		//Looks for the value with the highest value
	Math.min(x, y);		//Looks for the value of lower value
	Math.sqrt(x);		//Square roots
	Math.abs(y);		//Absolute value
	Math.round(y);		//Rounds value
	Math.ceil(y); 		//Rounds up
	Math.floor(y);		//Rounds down
